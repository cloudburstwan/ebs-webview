# EBS Webview Environment Variables

# The default stream name to load (e.g., 'test', 'pony')
VITE_DEFAULT_STREAM=test

# Comma-separated list of supported quality labels
VITE_SUPPORTED_QUALITIES=360p,720p

# Whether to perform pre-validation of source availability (true/false)
VITE_VALIDATE_SOURCES=false

# Whether to filter out withheld streams in the dropdown (true/false)
VITE_FILTER_STREAMS=true

# The base URL for the stream backend
VITE_STREAM_BASE_URL=https://stream.equestria.horse

# The suffix appended to the base URL for stream access (default: 's')
VITE_STREAM_SUFFIX=s

# The API version path
VITE_API_VERSION=api/v1
